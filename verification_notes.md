# 実装時の確認記録

確認日：2026-08-13

## 画面サイズ

初期値を「16:9、対角200 inch」とした場合、横幅4,428 mm、高さ2,491 mm、面積11.03 m²と表示された。計算値は 200 inch × 25.4 mm/inch を基にした幾何計算と一致する。

## 投影照度

初期条件を「環境光200 lx、面積11.03 m²、10,000 ANSI lm、実効係数0.8」とした場合、スクリーン照度907 lx、5:1に必要なプロジェクター輝度13,788 ANSI lm、10:1に必要なプロジェクター輝度27,575 ANSI lmと表示された。

プロジェクター輝度を15,000 ANSI lmに変更した場合、スクリーン照度は1,360 lx、環境光に対する比率は6.8:1となり、5:1の判定が「目標を満たす」、10:1の判定が「目標に不足」に即時切り替わることを確認した。

## スローレシオ

初期条件を「引き距離8.00 m、16:9、画面横幅4,428 mm」とした場合、必要スローレシオは1.81:1、画面高さは2.491 m、対角は200.0 inch、面積は11.03 m²と表示された。横幅入力から対角インチ入力への切替では、現在の画面サイズを200.0 inchとして引き継ぐよう修正した。選択メニューの変更についても、各計算機の入力更新を確実に行う処理を追加した。

修正後、対角インチ入力へ切り替えた際にラベルが「必要な対角サイズ」、単位がinch、入力値が200.0となり、スローレシオ1.81:1と画面寸法が維持されることを確認した。

## 2面ブレンド

16:9、1台分の画面高さ2.50 m、解像度1,920×1,080 px、ブレンド10%の初期条件では、1台分の投影幅4.44 m、ブレンド幅192 px、全体投影幅8.44 m、全体キャンバス3,648×1,080 px、全体面積21.11 m²と表示された。

ブレンド幅をピクセル指定に切り替え、10 pxとした場合は、ブレンド率0.5%、全体投影幅8.87 m、全体キャンバス3,830×1,080 pxに更新されることを確認した。

## 映像容量

2面ブレンドの「解像度を映像容量へ」操作により、全体キャンバス3,830×1,080 pxが非圧縮映像とMP4の両方へ反映されることを確認した。映像尺1時間、59.94 fps、YUV 4:2:0・10 bit、PCM 48 kHz・24 bit・2 chの条件では、非圧縮合計容量1.67 TB、映像データレート3.72 Gbpsと表示された。

同条件でアルファチャンネルを有効にした場合、合計容量は2.79 TB、映像データレートは6.20 Gbps、1フレーム容量は12.93 MBとなり、アルファ1チャンネル分が加算されることを確認した。MP4は映像40 Mbps、音声320 kbps、コンテナ余裕2%で18.51 GBと表示された。

## 表示

デスクトップ幅では左側の計算機セレクター、右側の入力・結果パネルにより、入力と結果の視線を分離できている。結果値は濃紺のパネル上に大きく表示され、単位とともに識別できる。

390×844 pxのAndroidスマホ相当表示では、計算機セレクターを横方向のナビゲーションへ変え、結果パネルを入力パネルより先に表示する構成を確認した。主要な数値、単位、連携ボタン、入力欄は横方向の画面外にはみ出さず、比率比較表のみカード内の横スクロールとして保持される。

## 修正版：RGB表示とコピー対象

各計算機のヘッダーに「コピー対象」を追加し、画面サイズでは対角・横幅・高さ・面積・アスペクト比、映像容量では映像尺・非圧縮容量／データレート・MP4推定容量がコピー対象として表示されることを確認した。

映像容量計算でRGBを選択した場合、YUV専用のクロマサブサンプリング選択欄は非表示になり、「RGBは各ピクセルにR・G・Bの3成分を記録するため、クロマサンプリングは選択しません」という説明へ置き換わることを確認した。4K、59.94 fps、RGB・10 bit、アルファなしの初期条件では、映像データレート14.91 Gbps、合計容量6.71 TBと表示された。

## 見出し・説明文の更新

画面サイズツールの初期表示が「画面サイズを計算」、説明文が「対角インチ、横幅、または高さから、投影サイズと面積を換算します。」へ更新されていることを確認した。

投影照度ツールでは「スクリーン面積とANSIルーメンから照度を求め、環境照度の5～10倍を必要輝度としてプロジェクターを提案します。」と表示されることを確認した。2面ブレンドツールでは「2面の重複幅から、全体の投影サイズを確認」および「ブレンド幅の率かピクセル数を指定し、全体のスクリーンサイズと解像度を表示します。」と表示されることを確認した。
